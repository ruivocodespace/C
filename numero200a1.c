#include <stdio.h>
int main() 
{
int contador;

    printf("Números entre 200 e 1: \n");
        for(contador = 200; contador >= 1; contador = contador - 1)
        {printf("%i ", contador);}
    
    return 0;
}
