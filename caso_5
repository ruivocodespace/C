//caso_5
#include <stdio.h>

int main() {
char opcao;
    // Write C code here
    printf("Digite M(masculino) ou F(feminino):\n");
    scanf("%c", &opcao);
    
        switch (opcao) {
            case 'm':
            case 'M':
            printf("Masculino");
            break;
        
            case 'f':
            case 'F':
            printf("Feminino");
            break;
        
            default:
            printf("Opção inválida");
            break;
    }
    return 0;
}
