// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
int contador;


    printf("Números entre 10 e 200: \n");
        for(contador = 10; contador <= 200; contador = contador + 1)
        {printf("%i ", contador);}
    
    return 0;
}
